# usectl Landing — Project Log

Living project context. Read this first after `/clear` or in a new session — it has what `documentation.md` (the spec) doesn't: decisions already made, what's built, what's still open, and where the Figma data lives.

Repo: https://github.com/benqsj/usectl (branch `main`, pushed).

## Spec

`documentation.md` at repo root is the full technical spec (stack rules, architecture layers, animation philosophy, phased build order). Follow it. This file only tracks state and decisions *on top of* that spec.

## Figma

- File key: `uv4OBAdNJBr7GbSovRhnkj` (file: "Usectil-Landing")
- Design is a single fixed **dark theme**, not light/dark adaptive. Page background (`--background` in `globals.css`) is `#1e1d1d` (corrected 2026-09-16 — was `#0a0a0a`, a guess; user gave the exact hex, which also matches the grain-overlay rectangle's own fill color noted below).
- Canvas reference size: 1920 wide. **Content container convention changed 2026-09-16** (superseded, see "Conventions established" below): no longer `max-w-[1722px] mx-auto` (fixed content width, collapses to full-bleed below 1722px, margins balloon unbounded above 1920px) — now pure `vw`-based fluid insets (`5.15625vw` = `99px` at the 1920 reference on each side), so the layout scales proportionally at *every* viewport width instead of snapping at a breakpoint. Forced by a real bug: at non-1920 widths the old container and the (already-fluid) `BackgroundLines.tsx` grid drifted apart, causing visibly asymmetric left/right margins on the user's laptop screen.
- User supplied two reference screenshots in `public/figma/`: one is a tight crop of the actual rendered header (trustworthy — matches real node data), the other is a zoomed-out Figma **editor** canvas screenshot with Figma's own toolbar visible in it. The latter may partly be showing Figma's own canvas/alignment grid rather than exported design content — treat it as directional, not pixel-source-of-truth. The precise numeric line data (82px pitch, x=99–1820.8) came from `get_metadata`, which is authoritative.
- **Figma MCP is rate-limited** (View seat on a Professional plan, not Editor) — tool calls run out fast. Batch `get_design_context` calls, don't re-fetch nodes already logged below.

### Known node IDs (page `0:1`, top-level frame `26:28` "FullLanding", h=13528; a duplicate frame `31:5803` exists offset at x=1960, same content)

| Node | Name | What |
|---|---|---|
| `26:29` / `31:5804` | Rectangle 1 | Full-page grain/noise overlay, `#1e1d1d` `mix-blend-multiply` + feTurbulence filter. Exported as `public/background/background-lines.svg` (1920×13528). |
| `26:31`–`26:52` / `31:5806`–`31:5827` | Line 75–97 | Vertical column-grid hairlines, full page height, spaced **82px apart**, spanning the content container (x=99 to x=1820.8 in canvas coords, i.e. edge-to-edge of the `max-w-[1722px]` container). Exact stroke color/opacity **not confirmed** (rate-limited before I could query it) — implemented as `rgba(255,255,255,0.04)`, 1px (lowered from `0.08` per user feedback 2026-09-16, "too strong"). **Verify against Figma and correct if wrong.** A few of the middle lines (`26:39`–`26:44`) start at y≈173 instead of y=0 (hidden behind hero content up top) — not replicated; current implementation runs all lines full height. |
| `26:53` | Line 97 | Horizontal header/content divider at y=96, full width. |
| `26:186` | Group 1 | Header logo (usectl wordmark), x=106 y=39, 150×24. Matches `public/logo/logo.svg`. **Implemented offset now diverges from this**: user asked to nudge it further right on 2026-09-17, now `130px`-at-1920 (`Header.tsx`) instead of the Figma-exact `106px` — deliberate, not a bug. |
| `26:190` | Group 1564 | Header nav links: The Machine, Agents, Features, Pricing, Documentation. Space Grotesk 16px, `rgba(255,255,255,0.8)`, items spaced with a consistent 32px gap. |
| `26:3243` area (~y=13259) | Footer | Footer content exists (nav columns, address, email, phone, newsletter signup with checkbox) — **not yet built**. |

Header frame in Figma has **no CTA button / no hamburger icon** on the right — confirmed by scanning all nodes in the y=0–96 band. Current header is logo (left) + nav (absolutely centered) with empty space right of nav. Flag this to the user if a CTA was intended but missing from the exported frame.

## Stack (installed)

Next.js 16 (App Router, Turbopack) · React 19 · TypeScript (strict) · Tailwind CSS v4 · GSAP 3 + `@gsap/react`. No Lenis, no Three.js/R3F yet — not justified until a section needs them (per spec §2/§18).

## Implemented so far

- Project scaffold: folder structure per spec §3 (`components/{layout,sections,ui,animations}`, `animations/`, `scenes/`, `hooks/`, `lib/`, `types/`).
- `src/app/layout.tsx` — root layout, SEO metadata skeleton (title/OG/Twitter all `TODO:` placeholders — need real copy), fonts: Geist (default), Space Grotesk (`--font-heading`, used for nav).
- `src/app/globals.css` — dark theme tokens, `prefers-reduced-motion` baseline.
- `src/components/layout/Header.tsx` — Server Component, sticky, logo + centered nav (`absolute left-1/2 -translate-x-1/2`), nav hidden below `md` (no mobile menu built yet — Figma header has no hamburger, so this is a gap, not a deliberate call to skip mobile nav). **Background changed 2026-09-17 — supersedes the "fully transparent, no background patch" decision below**: header is now `bg-background/60 backdrop-blur-md` (a translucent dark tint + glassmorphism blur), per explicit user request ("header-ს roca mogyveba blur daadeb background-ze" — give the sticky header a nice blur on its background). Verified via Playwright screenshots at scroll-top and mid-scroll (1920×500, scrolled to y=700) — content (hero cube, grid lines) visibly blurs/glows behind the header while scrolling, looks correct in both states. The paragraph below (about the header/logo/nav being fully transparent so grid lines show through unmasked) is now historical context for *why* transparency was originally chosen, not the current behavior — the grid-line masking logic in `BackgroundLines.tsx` is unaffected either way, since it doesn't depend on the header's own background. **Container rewritten 2026-09-16**: inner row is no longer `mx-auto max-w-[1722px] ... lg:pl-[106px]` (fixed px, only applied at the `lg` breakpoint) — now `paddingLeft`/`paddingRight` in `vw`, applied uniformly at every width, no breakpoint gating. This keeps the logo's position always in sync with `BackgroundLines.tsx`'s own `INSET_VW`/hide-column math (see below) — they were both anchored to the 1920 reference before, which only actually matched at exactly 1920px viewport width. `paddingLeft` is `6.770833vw` (`130px` at 1920, nudged right from an initial `106px`/`5.520833vw` per user request 2026-09-17 — "move the logo a bit more to the right"), `paddingRight` stays `5.15625vw` (`99px` at 1920, matches the content inset).
- `src/components/layout/BackgroundLines.tsx` — Server Component, `position:absolute inset-0 -z-10` inside `<body>` (body is `relative`), renders the grain texture (`background-lines.svg`) + a grid of lines — vertical **and horizontal**, now as **two separate divs** (split 2026-09-16, previously one div with two layered gradients):
  - **Vertical (column) lines**: bounded to a `vw`-fluid inset container (`left/right: INSET_VW` = `5.15625vw`, matches Figma's x=99..1820.8 span at the 1920 reference — **not** `mx-auto max-w-[1722px]` anymore, see below), pitch `82px`-equivalent (`vw`-scaled, Figma-confirmed at reference). Split into **two divs** (2026-09-16): a `h-24` (96px, fixed) header band with the logo/nav mask applied, and a second div below it (`top: 96px` to `bottom: 0`) with no mask at all (nothing to hide there) — needed because the mask's Y-axis math (below) requires a target box with a *known, stable* height, which the old single full-page-height div didn't have. **Right-edge line missing, fixed 2026-09-16**: at the 1920 reference width, `1722/82 = 21` exactly, so the pattern's last repetition falls precisely on the container's right boundary — a CSS gradient line starting exactly at an element's edge has 0 visible width (classic fencepost/off-by-one clipping), so the rightmost vertical line was fully invisible while the leftmost (at local x=0) was fully visible. Fixed by shifting the whole pattern left `backgroundPositionX: -${LINE_THICKNESS_PX/2}px` (half the line thickness) — both edges now show a symmetric partial line instead of one full + one missing. Verified with a pixel-level measurement (see below): before the fix, 21 lines detected (x=99 to x=1739); after, 22 lines (x=99 to x=1820), matching Figma's authoritative x=99..1820.8 span almost exactly. **Asymmetric left/right margins on non-1920 screens, fixed 2026-09-16**: root cause was the container itself — `mx-auto max-w-[1722px]` caps CONTENT width at a fixed 1722px and lets margins absorb any viewport difference, which is a completely different scaling behavior than the (already `vw`-fluid) line pitch. Below 1722px viewport the container went full-bleed (0 inset) while pitch kept shrinking proportionally; above 1920px margins grew unboundedly instead of the whole grid scaling up. User saw this as "big gap on the left, tiny gap on the right" on their laptop. Fixed by replacing the container with pure `vw` insets (`INSET_VW`), matching `Header.tsx`'s new padding (see above) — verified symmetric (within 1px rounding) at 1920, 1512, and 1440 via pixel measurement.
  - **Horizontal (row) lines**: full viewport width, `inset-x-0` (literally `left:0`/`right:0`, edge-to-edge — corrected 2026-09-16, was previously bounded to the same 1722px container as the vertical lines, which cut them short of the true page edges). Pitch `114px` (corrected 2026-09-16 from an earlier `96px` guess). Starts at `top: ROW_GRID_TOP` = `calc(96px + ROW_PITCH)` — i.e. **no horizontal line renders inside the header row at all**; the header's own `border-b` is the only line at that seam. **First-gap-vs-rest mismatch, fixed 2026-09-16**: the offset used to be a plain JS-computed constant `96 + 114 = 210px` — correct only exactly at the 1920 reference width, since `114` (the value baked into that constant) doesn't itself scale with viewport the way `ROW_PITCH` (vw-based) does for every *later* gap. At any other viewport (e.g. a 1512px laptop screen), the header-to-first-line gap stayed a fixed 114px while every subsequent gap shrank proportionally (~90px at 1512), so the first gap visibly didn't match the rest — exactly the user's report. Fixed by using `calc(96px + ${ROW_PITCH})` instead of a precomputed px number, so the first gap is always exactly one (fluid) `ROW_PITCH`, matching every later gap at any viewport width. Verified by measuring pixel rows at both 1920 (gaps: 114,114,114,114,114) and 1512 (gaps: 90,90,90,89,90,90) — uniform at both.
  - **Fluid scaling**: both pitches are expressed in `vw`, derived from a `CANVAS_WIDTH_REF = 1920` (Figma canvas reference) via a `vw()` helper — e.g. `82px` becomes `4.27vw` — so the grid scales proportionally on smaller screens instead of snapping at a breakpoint (per user request 2026-09-16). **Uneven-crispness tradeoff, addressed 2026-09-16**: `vw`-based pitch puts hairlines at non-integer physical pixel positions, so at 1px thickness the browser anti-aliased each line slightly differently (some crisper/darker, some fainter) — confirmed visible at higher test contrast (0.15) via a Playwright screenshot. Asked the user; they chose to widen the lines rather than give up fluid scaling, so `LINE_THICKNESS_PX = 2` now (was a `1px` hairline) — thicker lines make the sub-pixel anti-aliasing variance much less noticeable, confirmed by another Playwright screenshot.
  - Line color is very faint: `rgba(255,255,255,0.02)`.
  - **Logo/nav masking — abandoned CSS masks entirely, 2026-09-16, after 4 failed attempts**: (1) `mask-composite: exclude` multi-layer, (2) `mask-image: url(data:image/svg+xml,...)`, (3) inline `<svg><mask>` with `userSpaceOnUse` (fixed the "masks everything" bug — a `width="100%"` rect resolving to zero size — but user still reported lines visible), (4) inline `<svg><mask>` with `objectBoundingBox` fractions (fixed the *reasoning* — hole width shouldn't scale with viewport since the logo/nav's actual pixel width is fixed, not `vw` — but still reported not working). All four looked correct in code, server HTML, and Playwright screenshots at every width tested, yet the user kept seeing lines through the logo/nav in their real browser — never fully root-caused (possibly a browser/engine difference, possibly something about how the mask combines with the `vw`-fluid line pitch that a Chromium screenshot didn't surface). **Current approach**: a shared `ColumnLines` helper component renders the vertical lines as individual numbered `<div>`s (`data-col={1..22}`, 1-indexed left→right) instead of a CSS background pattern — each column's `<div>` is conditionally given `min-[1800px]:hidden` / `max-[1799px]:hidden` (literal Tailwind class strings — **not** built via template interpolation, since Tailwind's build-time scanner can't see dynamically-constructed class names and would silently emit no CSS). Column sets: `START_COLUMNS = {2,3}` (under the logo — **computed**, not user-confirmed, from the logo's fixed-px box vs. where columns land at 1920/1800/1440px), `MIDDLE_COLUMNS_WIDE = {9..14}` / `MIDDLE_COLUMNS_NARROW = {8..15}` (under the nav text — **user-counted by eye**, trustworthy). `ColumnLines` is used **twice**: once for the header band (`HEADER_HIDE_*` = start ∪ middle — hides both logo and nav columns), and once more for a band **exactly one `ROW_PITCH` tall directly below the header** (`top: 96px, height: ROW_PITCH`, `BELOW_HEADER_HIDE_*` = middle only). Below *that* the page reverts to a **plain, full, unbroken grid** (a normal `repeating-linear-gradient` div, no hidden columns at all) — corrected 2026-09-17 from an initial version that hid the middle columns for the *entire* rest of the page; the user only wanted the nav-width gap to continue for its first occurrence right below the header, not as a permanent channel down the whole page. This is deliberately simple/debuggable per the user's own request ("give me a class I can hide things with") after the mask approaches proved unreliable in ways that couldn't be diagnosed from this end.
  - **Verification method**: installed Playwright's Chromium locally (`npx playwright install chromium`) and screenshotted `localhost:3000` directly (at a temporarily bumped opacity for visibility, then reverted) — this is now the go-to way to visually verify CSS changes in this project when `claude-in-chrome` isn't connected; don't trust "looks right in the rendered HTML" without an actual screenshot, per the two prior mask failures. Went one step further 2026-09-16 for the edge/gap bugs above: a pure PNG-pixel-row/column brightness scan (small hand-written script, stdlib `zlib`+`struct` only, no Pillow available) to get exact line positions/gaps as numbers instead of eyeballing a screenshot — much more reliable for "is this gap exactly Npx" questions than visual inspection, especially at the very low production opacity (0.02) where lines are barely visible to the eye at all. Worth reaching for again for any future pixel-exact grid/spacing claim.
- `src/app/page.tsx` — renders `<HeroSection />` then `<InfrastructureSection />` (2026-09-17).
- `src/components/sections/InfrastructureSection.tsx` — second section, built from user-supplied screenshots (no Figma node given). Card is inset like the rest of the page's content (`mx-auto max-w-[1722px]`, later changed to a percentage width — see below). Left/right layout via flex (`md:flex-row`, text `flex-1`, graphic `shrink-0`) rather than a 50/50 grid — needed once the heading grew to `136px` (see below), a strict grid-cols-2 split wasn't wide enough for it to stay on one line. All text uses `font-heading` (Space Grotesk) per explicit user request 2026-09-17.
  - **`CardCross` corner marks removed, 2026-09-17 — explicitly temporary** (same request/batch as the hero crosses above): the card previously had a `CardCross` (reused `herosection/cross.svg`) at each of its 4 corners, centered exactly on the corner point via `top/bottom/left/right: -16.5px` (half the icon's 32px size). Deleted the `CardCross` component and its 4 render calls entirely — `InfrastructureSection.tsx` currently has **zero** references to `cross.svg`. **Exact removed code, for restoration**:
    ```tsx
    function CardCross({ position }: { position: "top-left" | "top-right" | "bottom-left" | "bottom-right" }) {
      const vertical = position.startsWith("top") ? { top: -16.5 } : { bottom: -16.5 };
      const horizontal = position.endsWith("left") ? { left: -16.5 } : { right: -16.5 };
      return (
        <Image
          src="/herosection/cross.svg"
          alt=""
          width={32}
          height={32}
          aria-hidden="true"
          className="absolute"
          style={{ ...vertical, ...horizontal }}
        />
      );
    }
    ```
    Rendered (as the first children right inside the card's outer `<div className="relative mx-auto w-[85%] border ...">`, before the flex row) as:
    ```tsx
    <CardCross position="top-left" />
    <CardCross position="top-right" />
    <CardCross position="bottom-left" />
    <CardCross position="bottom-right" />
    ```
    To restore: re-add the `CardCross` component and these 4 calls back into `InfrastructureSection.tsx`, right after the card's outer `<div>` opening tag.
  - **Card border, tried a chamfered top-left corner then fully reverted, 2026-09-17**: went from plain border → chamfer-all-4-corners (`clip-path: polygon` w/ 8 points) → chamfer-top-left-only (5 points) → user decided against any cut corner at all, back to a **plain rectangular `border border-white/10`, no `clip-path`, no radius**. Along the way, confirmed (by temporarily bumping to `border-white/60` and screenshotting) that the border was never actually broken — at the real 10% opacity it's just very faint against the grain texture on straight edges, a perception issue, not a bug. If a cut-corner border is ever wanted again, the working 5-point `clip-path: polygon(Npx 0, 100% 0, 100% 100%, 0 100%, 0 Npx)` formula from this exploration is still valid.
  - **Heading is `136px`**, wraps to exactly 2 lines ("You came" / "here to build.") via `max-w-[950px]` — reverted from an earlier `text-nowrap` (one-line) version once the user clarified they *do* want it to wrap, just not via a manual `<br>`. Paragraph is `32px`, `max-w-[860px]`, wraps to 4 lines with an exact user-specified break after each of "...The idea you" / "...handles the" / "...move your" / "product forward" — this same `860px` value happened to already produce exactly that 4-line breakdown from an earlier tuning pass, no further adjustment needed. All these widths are pure trial-and-error against a screenshot, not derived from anything.
  - **Assets, finalized 2026-09-17**: the actual directory the user created is `public/infrastructur/` (**missing the final "e"** — not a typo to "fix", this is the real path in both the filesystem and the code; confirmed via `ls` before trusting it) containing `server.svg` (unused), `servertitanium.svg` (568×556, now used for the right-side graphic — a much more tightly-stacked 4-layer look than `herosection/servers.svg`, which is no longer referenced anywhere in this section), and `static.svg` (821×35, a hairline-ruler/waveform graphic with scattered green accent ticks — replaces the earlier hand-built waveform `<div>` bars entirely, which have been deleted). Always verify with `ls public/infrastructur/` before assuming an asset is present — twice now the user described files that weren't actually uploaded yet when they said they would be.
  - **`servertitanium.svg` widened to `650px` on FullHD only, 2026-09-17** (`min-[1800px]:w-[650px]`, up from `320px`), explicitly scoped to not disturb the `md`-to-`<1800px` size. **Found and fixed a real Tailwind cascade bug while doing this**: `md:w-[320px] min-[1800px]:w-[650px]` silently did *not* apply the 650px width at 1920px — both media queries are simultaneously true there (1920 ≥ 768 **and** ≥ 1800), and Tailwind emitted the `md:` rule *after* the `min-[1800px]:` rule in the compiled stylesheet, so `md:` won the cascade regardless of source order in the `className` string. Confirmed via `getComputedStyle` before and after (`320px` → still `320px` with the naive fix). Fixed by making the ranges mutually exclusive instead of overlapping: `md:max-[1799px]:w-[320px] min-[1800px]:w-[650px]` — now each rule only ever matches one non-overlapping range, so cascade order can't matter. **Applies generally**: any time two Tailwind breakpoint variants can both be true at the same viewport width (a `min-[Npx]:` custom breakpoint co-occurring with a named breakpoint like `md`/`lg`), don't assume writing the "more specific" one later in the class string wins — verify the actual computed style, and if wrong, make the ranges exclusive with `max-[(N-1)px]:` rather than relying on source order.
  - **Card width now percentage-based, 2026-09-17**: was `max-w-[1722px]` (fixed, so below ~1770px viewport it just went edge-to-edge minus the section's `px-6`, e.g. ~96.7% of viewport at 1440px — nowhere near the intended proportional inset). User wanted the FullHD ratio (`1722/1920 ≈ 89.7%`, described as "90%") preserved but a *different*, more generous ratio (`85%`) on smaller screens. Now `w-[85%] min-[1800px]:w-[1722px]` (base + one override, no cascade-ambiguity risk since there's no third overlapping breakpoint here). Also had to remove the `<section>`'s own `px-6` — `w-[85%]` was resolving against the section's *padded* content box, not the true viewport, giving `82.2%` instead of `85.0%` at 1440px; removing the redundant section padding (the card's own percentage width already provides the horizontal inset at every size) fixed it to exactly `85.0%`, confirmed via `getComputedStyle`. Checked for horizontal overflow at 500px — none.
  - **`servertitanium.svg` moved down `100px`, 2026-09-17**: plain `mt-[100px]`, applied at all widths (not breakpoint-scoped — not asked to be).
  - **Card top snapped to a grid row line on narrow screens, 2026-09-17**: user wants the card sitting exactly on one of `BackgroundLines.tsx`'s horizontal grid lines when viewed below FullHD (clarified via a follow-up question — the ambiguous "also raise it up slightly" from the original request turned out to be about the *card*, not the server graphic). Measured the card's natural top position at 4 different narrow widths (1024/1200/1440/1700) and found it identical (`y=1001.984375`) at all of them — confirms (same as the hero section's `CUBE_TOP_Y` finding) that everything above it in the narrow bucket uses fixed px, not `vw`, so a single fixed adjustment works across the whole bucket. Nearest row line below that position (using the `rowY()`/`ROW_PITCH` grid math at 1440px specifically) is row `10`, at page-`y=951` — added `mt-[-51px] min-[1800px]:mt-0` to the `<section>` to close that ~51px gap. Verified pixel-exact at 1440 (card top now `950.98`, target `951`) via a high-contrast grid screenshot — the card's border and a grid line visibly coincide. **Caveat inherited from the same fixed-vs-fluid tension documented elsewhere in this file**: because the target row line is `vw`-fluid but the `-51px` adjustment is a flat constant, this is only pixel-exact at the width it was measured against (1440) — at very different narrow widths (e.g. 1024) the row-line target itself moves to a different absolute position, so the alignment will drift somewhat. Not fixed further since the user's ask was "slightly" (implying a small, one-off nudge, not a fully fluid recalculation) and this matches their explicit testing width.
  - Copy is typed exactly as shown in the screenshot except the eyebrow's spelling, which the screenshot mockup has as "Infrastucture Freedom" (missing the second "r") — assumed a typo in the design and spelled correctly here ("Infrastructure Freedom"). Flag if that assumption is wrong.
  - Not yet verified: padding/chamfer size are estimated by eye from the screenshot (same caveat as the hero section — no Figma access to confirm against).
- `src/components/sections/HeroSection.tsx` — first section built. Eyebrow row (`public/herosection/Subtract.svg` icon + "Managed Kubernetes & AI Agent Infrastructure"), big centered `<h1>` ("One server. **Unlimited** machines." — "Unlimited" in `text-brand`), body paragraph, two pill `Button`s ("Create Machine", "See how it works" with a trailing arrow icon). **Built from a user-supplied screenshot + a generic Figma link (`node-id=0-1`, the page root, not the actual hero frame) — Figma MCP is still rate-limited (same View-seat limit noted throughout this doc), so nothing here has been cross-checked against real Figma node data.** **Font sizes are fixed px, user-specified exactly (2026-09-17), not fluid/clamped**: eyebrow `text-[22px]`, `<h1>` `text-[98px]` (was a `clamp()` fluid value — replaced per explicit instruction, so it no longer scales down on narrower screens; `sm:text-nowrap` still forces one line at `sm`+ widths), paragraph `text-[28px]`. Paragraph also has `max-w-[1080px]`, tuned by hand (not derived from anything) so it naturally wraps right after "...take your idea" / before "live, without a DevOps team..." — matching the reference screenshot's line break **without a manual `<br>`**, purely via width-driven wrapping; if the copy ever changes, this max-width will need re-tuning to keep that break point. Eyebrow icon (`Subtract.svg`) renders at its **native size, `58×26`** (confirmed by user 2026-09-17 — was `44×20`, scaled down). Eyebrow **text** typography fully user-specified 2026-09-17: `font-heading` (Space Grotesk — the `Space_Grotesk()` call in `layout.tsx` has no `weight` array, so it loads as a variable font and any weight in its axis renders correctly), `font-light` (300), `leading-none` (100% line-height), `tracking-[-0.02em]` (-2% letter-spacing) — verified via `getComputedStyle` (`fontWeight: 300`, `lineHeight: 22px`, `letterSpacing: -0.44px`, i.e. exactly -2% of the 22px size). Below the buttons: a static 3D server-cube graphic (`public/herosection/frame_1_7.svg`, renamed 2026-09-17 from `frame_1 7.svg` — a space in a filename needs URL-encoding to be referenced from `src`, simpler to just rename). **Two candidate SVGs were compared** (both in `public/herosection/`, user-supplied 2026-09-17): `frame_1_7.svg` is actually a single base64-embedded PNG wrapped in an SVG `<pattern>` (not real vector paths — confirmed by grepping for `<image`/`<g id=`) but visually matches the reference screenshot's **closed/assembled** cube exactly. `servers.svg` (568×742) *is* genuine vector — real `<path>` elements — but renders as an **already-exploded** view (server layers separated vertically with dashed connector guide-lines baked into the file), not the closed cube. Discussed with the user: reassembling `servers.svg` into a closed static state by hand (shifting its already-separated layer paths back together, guessing at offsets) was considered and rejected as too fragile/inaccurate without real per-layer transform data. **Decision**: keep `frame_1_7.svg` for accurate static display; keep `servers.svg` as the future vector source once the scroll-driven disassembly animation is actually built (per spec's animation-system phase, not started yet) — its layers are already separated, which should make that animation considerably easier than starting from a flat raster. **The user does not want that animation built yet** — this is purely a heads-up for a future session, not a pending task with a deadline. **Actually rendered asset switched to `public/herosection/server-cube.png` 2026-09-17**: `frame_1_7.svg`'s embedded PNG has huge asymmetric transparent padding baked in (measured via a pixel-content bounding-box scan: visible content only spans y=310..1487 and x=136..1463 of the 1600×1600 canvas — 310px of empty space above the cube, only 113px below). Removing the `<Image>`'s own `mt-*` class didn't fix the "too far from the buttons" gap the user reported, because the gap was baked into the image's own transparent margins, not CSS margin. Fixed by writing a one-off crop script (reusing the pixel-decode helper from the grid-lines debugging work, plus a matching minimal PNG encoder) to crop tightly to the content bbox (+20px buffer) and save as `server-cube.png` (1367×1217) — referenced directly (no SVG wrapper needed). `frame_1_7.svg` itself is left in place, just unused now. **Corner `+` marks, added then re-snapped 2026-09-17**: `public/herosection/cross.svg` (32×32 crosshair) at 4 positions inside the same `relative w-full max-w-[900px]` wrapper. First pass was an eyeballed approximation (breakpoint-based `left-4/sm:left-12/md:left-20` etc. — the reference screenshot came from a macOS `TemporaryItems` path that got purged before it could be pixel-measured, unlike the grid-lines saga where the file survived long enough). User then asked for them to sit exactly on `BackgroundLines.tsx`'s grid intersections, nudged slightly inward from that first pass. **Exact math** (verified 2026-09-17 by re-enabling the grid at high test opacity, measuring the wrapper's `boundingBox()` via Playwright, then cropping+zooming into each corner with the same pixel-decode/encode helpers used elsewhere — crosshair center lands exactly on a line crossing at all 4 corners): at the 1920 reference width, column line x-positions are `INSET_VW + i·COLUMN_PITCH` (`i` = column index, 0-based) and row line y-positions are `HEADER_HEIGHT_PX + j·ROW_PITCH`. Chose column `i=7` (x=673px) and its mirror `i=14` (x=1247px, since `21-7=14`) for left/right, and row `j=3` (y=438px, above the cube) and `j=7` (y=894px, below it) for top/bottom. Converted to wrapper-relative fixed px (wrapper measured at `x=510,y=490.89,w=900,h=427.3`): `left-[146px]`/`right-[147px]` and `top-[-70px]`/`top-[387px]`. That fixed-px version was exact only at 1920.
  - **Made genuinely fluid at any width, 2026-09-17** (user asked for other screen sizes to be covered too, plus nudged the crosses one column further out each side and one row down, and the cube 60px further down): extracted `src/lib/grid.ts` — `INSET_VW`, `COLUMN_PITCH`, `ROW_PITCH`, `HEADER_HEIGHT_PX`, plus new `columnX(i)`/`rowY(j)` helpers — out of `BackgroundLines.tsx` so both files import the *same* constants instead of each hardcoding a copy that could drift. `BackgroundLines.tsx` now imports these instead of defining them locally (pure refactor, no behavior change there). New column indices `6`/`15` (was `7`/`14`), new rows `4`/`8` (was `3`/`7`). The crosses are no longer inside the narrow `max-w-[900px]` cube wrapper — they moved to a new sibling `absolute inset-x-0 top-0` overlay spanning the section's **full width**, so `left: calc(columnX(i) - 16.5px)` is a genuine page-absolute position (no wrapper-offset correction needed horizontally). Vertically, `rowY(j)` returns `j·ROW_PITCH` measured *from the header's bottom edge* — this lines up perfectly with `top-0` on the overlay **only because** the overlay is a direct child of `<section>`, and `<section>`'s own top edge sits exactly at the header's bottom edge (`position:absolute` children ignore their containing block's own padding, so `section`'s `pt-16` doesn't shift this) — confirmed by measuring the wrapper's `y` at three widths (1920/1512/1024) and finding it identical (`490.890625` all three), which only holds because every bit of hero content above the cube uses fixed px, not vw, so nothing above it reflows-by-width (would break if the paragraph wraps to a 3rd line on very narrow screens, or the `sm:text-nowrap` heading wraps below the `sm` breakpoint — not handled, matches this hero's existing lack of real mobile optimization elsewhere). Re-verified pixel-exact (same zoom-crop technique) at both 1920 and 1512 post-change. Cube's own top margin is now `mt-[84px]` (was `mt-6`/24px, +60px per this request) — independent of the grid-snap math, just a flat additive shift.
  - **Rows split by breakpoint again, 2026-09-17**: user wanted *different* row positions for FullHD vs smaller screens — not achievable with one fluid `rowY(j)` formula, since that's inherently the same `j` at every width. Reused the same `>=1800px` cutoff already established for the grid's column-hiding logic (`HIDDEN_AT_WIDE`/`HIDDEN_AT_NARROW` pattern). Now renders **both** row-sets unconditionally (`WIDE_TOP_ROW=4`/`WIDE_BOTTOM_ROW=7` and `NARROW_TOP_ROW=6`/`NARROW_BOTTOM_ROW=11`) and toggles which set is visible via literal `hidden min-[1800px]:block` / `block min-[1800px]:hidden` classes (not template-interpolated — same Tailwind-scanner caveat as everywhere else in this doc). Columns (`6`/`15`) are unchanged, shared by both row-sets. Verified pixel-exact at 1920 (rows 4/7) and 1512 (rows 6/11).
  - **Columns split by breakpoint too, and cube width flattened to 2 tiers, 2026-09-17** (all scoped to the narrow/small-screen bucket only, per the user): `NARROW_LEFT_COLUMN=5` (was `6`, one further left), `NARROW_RIGHT_COLUMN=16` (was `15`, one further right, mirror of 5: `21-5=16`), `NARROW_BOTTOM_ROW=10` (was `11`, one row back up — net +2 from the original `8`). Wide/FullHD columns and rows (`6`/`15`, `4`/`7`) untouched. Cube width simplified from the old 3-tier `w-[320px] sm:w-[400px] md:w-[480px]` to 2-tier `w-[400px] min-[1800px]:w-[480px]` — flat `400px` for the entire narrow bucket (previously it stepped 320→400→480 across `sm`/`md`), `480px` only at `>=1800px`. Re-verified pixel-exact at 1512 with the new column/row numbers.
  - **Root-caused and fixed a real bug at 1680px, 2026-09-17** ("FullHD-ზე კარგია, პატარა ეკრანზე ცუდია" — user found it looked badly misaligned at 1680, between the two widths that had actually been checked): the grid-snapped approach above was fundamentally broken for a *range* of widths sharing one bucket. `columnX`/`rowY` are page-wide and `vw`-fluid — they resolve to a *different* absolute pixel position at every viewport width. The cube, meanwhile, has a *fixed* px width per bucket (400 or 480, flat, no longer scaling with viewport within a bucket after the tier-flattening above). So a grid column/row that happened to align with the cube's edge at 1512 (the one width actually checked) necessarily drifts away from it at any other width in the same 640–1799px bucket — 1680 just made this visible. **Rewrote positioning to be cube-relative instead of grid-relative**: horizontal position is `calc(50vw ± offset)` (50vw is the true page-center at *any* width; offset is a fixed px distance from the cube's own fixed-width edge, so the visual gap to the cube stays constant everywhere in a bucket) using a new `CubeCross` component (`side`/`hOffset`/`vOffset` props), replacing the old grid-relative `GridCross` (`columnX`/`rowY` from `src/lib/grid.ts` no longer used in this file — grid.ts itself is untouched, still used by `BackgroundLines.tsx`). Vertical position is a fixed px offset from `CUBE_TOP_Y = 490.890625` (the cube's page-y, constant across widths for the reasons noted above). All four offset constants (`WIDE_H_GAP=129`, `WIDE_TOP_OFFSET=61.109375`, `WIDE_BOTTOM_OFFSET=403.109375`, `NARROW_H_GAP=154.64`, `NARROW_TOP_OFFSET=143.759375`, `NARROW_BOTTOM_OFFSET=502.859375`) were *back-derived* from the exact pixel positions already verified-good at 1920 and 1512 respectively (not re-guessed), so the visual result at those two widths is unchanged — only the *formula* changed, from "same grid index everywhere" to "same offset-from-cube everywhere". Re-verified at 1920, 1512, **and 1680** (the reported-bad width) — all three now show the same consistent framing. **Lesson for next time**: when a fixed-size element and a fluid/vw-based system both need to visually relate to each other, they must share the *same* scaling behavior (both fixed, or both fluid) — matching them only at the specific width you happened to test is not sufficient, and it's usually not obvious until someone checks a third width.
  - **User rejected the cube-relative rewrite for the WIDE bucket, 2026-09-17**: "FullHD-ს თავი დაანებე, კარგად იყო" — even though the cube-relative formula was algebraically verified to reduce to the exact same pixel values at 1920 as the old grid-snapped one, they wanted the WIDE bucket reverted to the literal original `GridCross`/`columnX`/`rowY` implementation rather than trust the equivalence, and scoped the actual "still needs fixing" complaint specifically to **1440–1700px**. Current state, both approaches now coexist in the same file: **`GridCross`** (grid-snapped, `columnX(6/15)`/`rowY(4/7)`, imported from `src/lib/grid.ts`) for `>=1800px`; **`CubeCross`** (cube-relative, `calc(50vw ± offset)` + fixed px from `CUBE_TOP_Y`) for `<1800px`. Also found and fixed the actual root cause of the narrow bucket's "sunk down" complaint while re-deriving these: the *previous* narrow offsets (`NARROW_TOP_OFFSET=143.76`, `NARROW_BOTTOM_OFFSET=502.86`, back-derived from the old `row6`/`row10` grid values) put the bottom cross pair **~147px below the narrow cube's own bottom edge**, in empty space — that's what actually read as "sunk down," independent of the grid-vs-cube-relative bug. Replaced with offsets that mirror WIDE's own visual relationship to *its* cube exactly (`61.109375px` below the cube's top edge, `24px` above its bottom edge — same numbers as `WIDE_TOP_OFFSET`/`(cubeHeight - 24)`), scaled to the narrow cube's own height (`NARROW_CUBE_HEIGHT = 400 * 1217/1367 ≈ 356.15`, from the cube image's native aspect ratio). Horizontal gap (`NARROW_H_GAP = 154.64`) was untouched — the complaint was specifically vertical ("დაბლა" = down/low). Re-verified at 1920 (matches the restored-good state exactly), 1440, and 1700 — all three now show the same cube-framing relationship as WIDE.
  - **Narrow crosses removed entirely, 2026-09-17**: after all of the above, user decided to just not show the corner crosses below `1800px` at all, rather than keep tuning them. Deleted `CubeCross` and all `NARROW_*` constants (no dead code left behind) — `GridCross`/`columnX`/`rowY` for the `>=1800px` FullHD case is the only cross-rendering path left in `HeroSection.tsx`. `src/lib/grid.ts` is unaffected (still used here for the wide case, and by `BackgroundLines.tsx`).
  - **All remaining (wide/FullHD) crosses removed too, 2026-09-17 — explicitly temporary** ("cros.svg-ebi drobit moashore" — remove the cross.svg's *temporarily*): deleted the last cross-rendering path (`GridCross` component, its 4 render calls, the wrapping `<div aria-hidden className="pointer-events-none absolute inset-x-0 top-0">`, and the now-unused `import { columnX, rowY } from "@/lib/grid"`) from `HeroSection.tsx`. `HeroSection.tsx` currently has **zero** references to `cross.svg` or the grid-cross system. `src/lib/grid.ts` is untouched (still used by `BackgroundLines.tsx`). **Exact removed code, for restoration**:
    ```tsx
    import { columnX, rowY } from "@/lib/grid";

    const CROSS_HALF_PX = 16.5; // half of cross.svg's 32px size, to center it on a grid intersection
    const VISIBLE_AT_WIDE = "hidden min-[1800px]:block"; // literal class — see Tailwind-scanner caveat elsewhere in this doc
    const WIDE_LEFT_COLUMN = 6;
    const WIDE_RIGHT_COLUMN = 15; // mirror of 6 (21 - 6 = 15)
    const WIDE_TOP_ROW = 4;
    const WIDE_BOTTOM_ROW = 7;

    function GridCross({ column, row }: { column: number; row: number }) {
      return (
        <Image
          src="/herosection/cross.svg"
          alt=""
          width={32}
          height={32}
          aria-hidden="true"
          className={`absolute ${VISIBLE_AT_WIDE}`}
          style={{
            left: `calc(${columnX(column)} - ${CROSS_HALF_PX}px)`,
            top: `calc(${rowY(row)} - ${CROSS_HALF_PX}px)`,
          }}
        />
      );
    }
    ```
    Rendered (as a sibling of the eyebrow/heading/paragraph/buttons/cube, inside the `<section>`) as:
    ```tsx
    <div aria-hidden className="pointer-events-none absolute inset-x-0 top-0">
      <GridCross column={WIDE_LEFT_COLUMN} row={WIDE_TOP_ROW} />
      <GridCross column={WIDE_RIGHT_COLUMN} row={WIDE_TOP_ROW} />
      <GridCross column={WIDE_LEFT_COLUMN} row={WIDE_BOTTOM_ROW} />
      <GridCross column={WIDE_RIGHT_COLUMN} row={WIDE_BOTTOM_ROW} />
    </div>
    ```
    To restore: re-add the import, the 5 constants, the `GridCross` component, and the wrapping `<div>` with its 4 calls, back into `HeroSection.tsx` (right after the closing `</div>` of the buttons row is where it lived).
- Still estimated/unconfirmed: button pill styling (border-only, `hover:bg-white/5`). Verify the rest once Figma access returns, and get the user to confirm on their own screen (same lesson as the grid-lines saga — Playwright screenshots here at 1920 and a 390px mobile width both look right, but that's not proof for everyone's browser). **Section top padding is deliberate, not arbitrary** (2026-09-17, currently `pt-16` — nudged from an initial `pt-11` after the icon/font-size changes shifted things slightly): tuned so the eyebrow row's vertical center lands inside `BackgroundLines.tsx`'s "hidden middle-columns" band (`y=96` to `y≈210`, one `ROW_PITCH` tall right below the header) — the user wanted the eyebrow sitting exactly where those vertical lines are hidden. Bottom padding stays generous (`pb-28 md:pb-36`) for balance; only the top was tightened. If `ROW_PITCH`/`HEADER_HEIGHT_PX` or the eyebrow row's size ever change, re-check this still lines up.
  - **False-positive debugging note**: while iterating on this, a Playwright screenshot at a temporarily-bumped grid-line opacity (0.6) made the heading and paragraph text look like they were overlapping. Measured actual DOM box positions directly (via a one-off `playwright` npm install + a small Node script using `page.locator(...).boundingBox()`/`evaluate(getComputedStyle)`) and confirmed a clean 40px gap — the "overlap" was the horizontal test grid-line crossing through that exact region, a visual illusion from the debug opacity, not a real bug. Lesson: when a high-contrast debug screenshot shows something that doesn't match the computed CSS, check computed layout directly (bounding boxes / computed styles) rather than trusting the debug render's visual read — the debug overlay itself can mislead, same category of issue as the grid-lines saga but the opposite direction (this time the *test setup* was the false signal, not the real CSS).
- `src/components/ui/Button.tsx` — new reusable pill-button component (`href`, `children`, optional `withArrow`), used by `HeroSection`. First component in `components/ui/`.
- `globals.css` — added `--brand: #11a32a` / `--color-brand` token (matches the logo's green, `public/logo/logo.svg`, and `Subtract.svg`'s fill) — use `text-brand`/`bg-brand`/`border-brand` for brand-green accents going forward instead of hardcoding the hex again.
- Removed create-next-app's default SVGs (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`) from `public/`.

## Open items / TODO

- Verify `HeroSection.tsx` against real Figma data once the MCP rate limit resets — see the "Implemented so far" note above for exactly what's unconfirmed (font sizes, spacing, button styling).
- `public/herosection/cross.svg` exists but isn't used anywhere in the code right now — **temporarily removed 2026-09-17** from both `HeroSection.tsx` (4 `GridCross` corner marks around the cube, wide/FullHD only) and `InfrastructureSection.tsx` (4 `CardCross` corner marks on the card). User asked to remove them "temporarily," implying they'll likely come back — exact removed code + restoration instructions are preserved in both files' entries above under "Implemented so far." Restore from there when asked, rather than re-deriving the positioning math from scratch.
- Footer — Figma content exists (`26:3243` area) but not requested/built yet.
- Verify vertical line color/opacity against actual Figma stroke (currently a guess: `white/2%`, 1px — lowered twice 2026-09-16 per user feedback).
- Verify the `82px`/`114px` grid pitch and `99px` edge inset against Figma directly (currently: 82px column pitch is Figma-confirmed from earlier metadata; 114px row pitch and the fluid-vw scaling approach are per user instruction 2026-09-16, not yet cross-checked against a fresh Figma render). Figma MCP hit its View-seat rate limit on 2026-09-16 mid-verification (`get_metadata`/`get_screenshot` calls blocked). Re-check once the limit resets.
- ~~The fluid `vw()` scaling in `BackgroundLines.tsx` only applies to the grid line pitch, not the content container~~ — **resolved 2026-09-16**: both `Header.tsx` and `BackgroundLines.tsx` now use the same `vw`-fluid inset convention, see "Conventions established" below.
- Playwright's Chromium is now installed locally (`~/Library/Caches/ms-playwright`) — use `npx playwright screenshot --viewport-size=W,H <url> <out.png>` to visually verify CSS/layout changes directly, instead of trusting "looks right in the rendered HTML/CSS" (see the mask bug above — code and server HTML looked completely correct while the feature was 100% broken in an actual browser).
- Confirm with the user whether the computed logo hide-columns (`2, 3` in `BackgroundLines.tsx`) actually cover it — unlike the nav columns (user-counted), these are calculated, not confirmed.
- The header-band vertical-line hide-list is only 2 discrete breakpoints (`<1800px` / `≥1800px`), not truly continuous — a viewport width well below the "narrow" bucket's likely reference point (e.g. a 13" laptop at ~1024–1280px logical width) may need more columns hidden than `HIDE_COLUMNS_NARROW` currently covers, per the same math that made the narrow bucket need more columns than the wide one. Revisit if the user reports lines still showing on a notably smaller screen than what they tested.
- Four different CSS-mask techniques for hiding grid lines behind the header logo/nav all failed in the user's real browser despite looking correct in code, server HTML, and Playwright screenshots at every tested width — never fully root-caused. Ended up abandoning CSS masks for a discrete numbered-`<div>` + explicit hide-list approach instead (see `BackgroundLines.tsx` note above). If mask-based hiding is ever revisited, get direct browser access (`claude-in-chrome` or ask the user what browser/OS they're on) before trusting a Playwright/Chromium-only verification again.
- Header: confirm whether a CTA button belongs on the right (currently empty).
- Mobile nav (hamburger menu) — not in the Figma header frame; ask before building one.
- SEO metadata (`layout.tsx`) still has placeholder `TODO:` copy — fill in once real product copy exists.
- Responsive pass (spec §14) and animation system (spec §4 Phase 4) not started — site is currently static, no scroll-driven behavior yet.

## Conventions established

- **Shared content inset — superseded 2026-09-16**: no longer `mx-auto max-w-[1722px] px-6 lg:px-[99px]`. Now pure `vw`-fluid: horizontal insets expressed as `vw` derived from a `1920` reference (e.g. `5.15625vw` for the `99px`-at-1920 content edge inset), applied uniformly at every viewport width with no breakpoint gating — see `Header.tsx`'s `paddingLeft`/`paddingRight` and `BackgroundLines.tsx`'s `INSET_VW` for the pattern. Reuse *this* for any new section, don't reintroduce the old `max-w` convention — it caused a real, user-reported bug (grid/header drifting apart, asymmetric margins) on non-1920 screens. If a section ever needs Figma-derived px positions inside its own content, convert with the same `vw(px) = (px / 1920) * 100 + 'vw'` helper, or a fraction (`px / referenceWidthPx`) if it needs to live inside an SVG/`objectBoundingBox` mask.
- Server Components by default; only mark `"use client"` where GSAP/interactivity is actually needed (spec §4).
- Update **this file** after any non-trivial change (new section, new dependency, new architectural decision, new Figma node explored) so a cleared session can resume without re-deriving context.
